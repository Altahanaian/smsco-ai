generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Signup {
  id    Int    @id @default(autoincrement())
  email String
  createdAt DateTime @default(now())
}
