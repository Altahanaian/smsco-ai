/** @type {import('next-intl').NextIntlConfig} */
module.exports = {
  locales: ['en', 'ar'],
  defaultLocale: 'en',
  // إذا لديك ملفات translation لكل صفحة، ممكن تضيف:
  // pages: {
  //   '*': ['common']
  // }
};
